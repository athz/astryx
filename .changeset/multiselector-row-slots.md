---
'@astryxdesign/core': patch
---

[feat] MultiSelector: expose theme targets for the dropdown option rows, per-row checkbox, row label, the Select All row, and the Select All divider, so themes can style them directly instead of relying on structural selectors. Purely additive — default appearance is unchanged.
@athz
